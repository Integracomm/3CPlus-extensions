{
  "name": "3cplus-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Extensao Chrome de click-to-call sobre o SDK 3C Plus",
  "scripts": {
    "test": "node test/ciclo-chamada.mjs && node test/painel.mjs",
    "build": "node build.mjs",
    "zip": "node build.mjs && powershell -Command \"Compress-Archive -Path manifest.json,src,vendor -DestinationPath 3cplus-extension.zip -Force\""
  },
  "dependencies": {
    "socket.io-client": "^4.7.5"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
